# Implementation Plan

- [x] 1. Sentry SDK のセットアップと初期化
- [x] 1.1 `@sentry/node` パッケージを Bot の依存関係に追加する
  - Bot パッケージに `@sentry/node` を production dependency として追加
  - _Requirements: 1.1_

- [x] 1.2 Sentry SDK 初期化モジュールを作成する
  - Bot 起動時に Sentry SDK を初期化する専用モジュールを作成
  - DSN、environment（NODE_ENV）、release（パッケージ名@バージョン）、tracesSampleRate を設定
  - `service: "bot"` タグをグローバルスコープに設定し、Web 側エラーと区別可能にする
  - DSN が未設定の場合は初期化をスキップし、Bot の正常起動を妨げない
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 4.1, 4.2, 5.1, 5.2_

- [x] 1.3 Bot エントリポイントに Sentry 初期化のインポートとシャットダウン時フラッシュを追加する
  - エントリポイントの最初のインポートとして初期化モジュールを読み込む
  - グレースフルシャットダウン処理で未送信の Sentry イベントをフ���ッシュする
  - _Requirements: 1.1, 4.3_

- [x] 1.4 Sentry 初期化のテストを作成する
  - DSN 設定時に SDK が正しいオプション（dsn, environment, release, tracesSampleRate, tags）で初期化されることを検証
  - DSN 未設定時に SDK 初期化がスキップされることを検証
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 5.2_

- [x] 2. エラー捕捉ユーティリティの作成
- [x] 2.1 (P) コンテキスト付きエラー報告ユーティリティを作��する
  - エラーの発生源（command, event, modal, task）、コマンド名/イベント名、ギルドID、ユーザーIDをコンテキストとして付与できるユーティリティ関数を作成
  - Sentry のスコープ機能でタグとコンテキスト情報を設定し、captureException でエラーを送信
  - Sentry が未初期化の場合は何もしない（no-op）
  - _Requirements: 2.1, 2.2, 3.1, 3.2_

- [x] 2.2 (P) エラー捕捉���ーティリティのテストを作成する
  - captureException がスコープ付きで正しく呼び出されることを検証
  - source, guildId, userId, name 等のコンテキスト情報がスコープに設定されることを検証
  - _Requirements: 2.1, 2.2, 3.1, 3.2_

- [x] 3. Bot コアへのエラー捕捉統合
- [x] 3.1 コマンド実行エラーとモーダルエラーの catch ブロックにエラー報告を追加する
  - スラッシュコマンド実行時のエラーを、コマンド名・ギルドID・ユーザーIDのコンテキスト付きで Sentry に報告
  - モーダル送信エラーも同様にコンテキスト付きで報告
  - 既存のユーザー向けエラーレスポンス（Discord への返信）は変更しない
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 3.2 イベントハンドラの catch ブロックにエラー報告を追��する
  - ギルド参加・脱退・更新イベントのエラーを、イベント種別とギルド情報のコンテキスト付きで Sentry に報告
  - _Requirements: 3.1, 3.2_

- [x] 3.3 Bot エラー捕捉統合のテ��トを作成する
  - コマンド実行エラー時にエラー報告ユーティリティが適切なコンテキストで呼ばれることを検証
  - イベントハンドラエラー時にエラー報告ユーティリティが呼ばれること��検証
  - エラー発生後もユーザー向けエラーレスポンスが正常に返されることを検証
  - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2_

- [x] 4. 環境変数ドキュメントの更新
  - Bot の環境変数テンプレートに `SENTRY_DSN` ��追加し、オプション項目であることを明記
  - _Requirements: 1.3, 5.1_
